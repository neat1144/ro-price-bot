// const sqlite3 = require("sqlite3").verbose();

// // Initialize an in-memory SQLite database
// const db = new sqlite3.Database(":memory:");

// // Close the database connection when Jest is done
// afterAll((done) => {
//   db.close((err) => {
//     if (err) {
//       return console.error(err.message);
//     }
//     done();
//   });
// });

// global.db = db;
